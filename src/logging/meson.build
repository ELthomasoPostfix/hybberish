# Library: The hybberish logging functionality.
logging_lib = library('logging', files(
                      'logging.c',
                      'extern_vars.c',
                    ),
                    include_directories : logging_dependencies_inc,
)
